apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciss-batch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ciss-batch
  template:
    metadata:
      labels:
        app: ciss-batch
    spec:
      containers:
      - name: ciss-batch
        image: ciss-batch:latest
        imagePullPolicy: IfNotPresent   # ← 로컬 이미지 사용 (중요)
        ports:
        - containerPort: 8083
        env:
        - name: SPRING_REDIS_HOST
          value: redis
        - name: SPRING_REDIS_PORT
          value: "6379"
---
apiVersion: v1
kind: Service
metadata:
  name: ciss-batch
spec:
  selector:
    app: ciss-batch
  ports:
  - port: 8083
    targetPort: 8083