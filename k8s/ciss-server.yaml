apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciss-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ciss-server
  template:
    metadata:
      labels:
        app: ciss-server
    spec:
      containers:
      - name: ciss-server
        image: ciss-server:latest
        imagePullPolicy: IfNotPresent   # ← 로컬 이미지 사용 (중요)
        ports:
        - containerPort: 8085
        env:
        - name: SPRING_REDIS_HOST
          value: redis
        - name: SPRING_REDIS_PORT
          value: "6379"
---
apiVersion: v1
kind: Service
metadata:
  name: ciss-server
spec:
  selector:
    app: ciss-server
  ports:
  - port: 8085
    targetPort: 8085